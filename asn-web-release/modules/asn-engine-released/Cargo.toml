[package]
name = "asn-engine-released"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
asn-core = { path = "../../../asn-core" }
asn-logger = { path = "../../../asn-logger" }
