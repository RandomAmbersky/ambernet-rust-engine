[package]
name = "triangle"
version = "0.1.0"
edition = "2021"

[lib]
name = "triangle"
path = "src/lib.rs"

[dependencies]
asn_render_webgl = { path="../../../asn_render_webgl" }
asn_core = { path = "../../../core" }
